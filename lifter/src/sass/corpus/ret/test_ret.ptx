.version 8.4
.target sm_89
.address_size 64

.visible .entry test_ret(.param .u64 input, .param .u64 output)
.maxntid 32, 1, 1
{
    .reg .b32 ri;
    .reg .b64 d1, d2;

    ld.param.u64 d1, [input];
    ld.param.u64 d2, [output];
    ld.u32 ri, [d1];
    st.global.u32 [d2], ri;

    ret;
}
